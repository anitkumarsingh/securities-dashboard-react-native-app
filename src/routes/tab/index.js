import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Tab = () => {
	return (
		<View>
			<Text>Tab</Text>
		</View>
	);
};

export default Tab;

const styles = StyleSheet.create({});
